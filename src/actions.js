export const increment = () => {
    return { type: 'INCREMENT' };
  }

 export const decrement = () => {
    return { type: 'DECREMENT' };
  }

// action creators: 
// const funkcja = () => ({ type: TYP_AKCJI, payload: PAYLOAD_AKCJI })
// zwraca:
// { type: TYP_AKCJI, payload: PAYLOAD_AKCJI }
//
//
